services:
  - type: web
    name: sih26082-air-intelligence-api
    runtime: python
    rootDir: application
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    plan: free
